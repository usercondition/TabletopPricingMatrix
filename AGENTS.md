# AGENTS.md

## Cursor Cloud specific instructions

This repository is a placeholder with no application code, package manifests, or services. The only tracked content besides docs is the `terst` file.

### Services

None. There is nothing to start, lint, test, or run as an app.

### Dependency refresh

The update/install script is a no-op (`true`). Do not add package installs until real project manifests exist.

### Smoke checks

- Confirm checkout: `ls -la` and `cat terst` (contents should be `test`).
- Confirm git: `git status` on the working branch.
